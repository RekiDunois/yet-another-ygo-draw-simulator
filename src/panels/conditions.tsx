import { Container, ContainerProps } from '@chakra-ui/react'

export interface ConditionsProps extends ContainerProps {}

export const Conditions = (props: ConditionsProps) => {
  return (
    <Container>
      conditions<p>panel</p>
    </Container>
  )
}
